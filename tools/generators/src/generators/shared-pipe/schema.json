{
  "$schema": "https://json-schema.org/schema",
  "$id": "SharedPipe",
  "title": "Genera un pipe standalone en libs/shared",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nombre del pipe",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "¿Nombre del pipe?"
    },
  },
  "required": ["name"]
}
